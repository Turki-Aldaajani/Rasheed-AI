# توثيق الهوية البصرية ونظام التصميم (Design System) لـ "رشيد"

يوفر نظام التصميم هذا لغة بصرية متكاملة وعالية التباين وتدعم الوصول السهل (Accessibility) والوضع الداكن (Dark Mode) بشكل تلقائي وتفاعلي بناءً على ألوان الهوية الحالية لـ "رشيد".

---

## 1. لوحة الألوان والتباين (Colors & Contrast)

تم تحديد وحفظ لوحة الألوان الحالية واستخراجها بدقة دون أي تغيير، مع تمكين الدعم للوضع الداكن على نفس هيكلية التوكينز.

### الألوان الأساسية لـ "رشيد" (Brand Greens)
- **الأخضر الداكن (`--color-brand-600`)**: `#1f6549` (اللون الرئيسي للهوية)
- **الأخضر المتوسط (`--color-brand-500`)**: `#357f5f`
- **الأخضر الفاتح (`--color-brand-100`)**: `#dcebe3`

### الألوان المحايدة (Ink Neutrals)
- **الداكن الأساسي (`--color-ink-900`)**: `#131816` (لون النص الأساسي في الوضع الفاتح)
- **الرمادي المتوسط (`--color-ink-600`)**: `#4d5652` (نصوص فرعية)
- **الفاتح المحايد (`--color-ink-50`)**: `#f7f8f8` (لون خلفيات العناصر)

---

## 2. الألوان الدلالية والوضع الداكن (Semantic Colors & Dark Mode)

تتغير قيم المتغيرات الدلالية تلقائياً بحسب الوضع (الفاتح/الداكن) لضمان أعلى مستويات التباين وسهولة الوصول (WCAG AA/AAA).

| المتغير | الوضع الفاتح (Light Mode) | الوضع الداكن (Dark Mode) |
| :--- | :--- | :--- |
| **خلفية الصفحة (`--color-bg-main`)** | `#ffffff` (أبيض) | `#131816` (رمادي داكن جداً) |
| **خلفية العناصر/البطاقات (`--color-bg-card`)** | `#ffffff` (أبيض) | `#252b29` (رمادي داكن متوسط) |
| **النصوص الأساسية (`--color-text-main`)** | `#131816` (داكن) | `#f7f8f8` (فاتح) |
| **النصوص الثانوية (`--color-text-secondary`)** | `#4d5652` | `#c4cbc8` |
| **الحدود (`--color-border`)** | `#e2e6e4` | `#39413e` |

---

## 3. الخطوط والطباعة (Typography)

- **الخط الأساسي (`--font-sans`)**: خط عربي أنيق مدمج مع خط النظام لضمان المقروئية والتحميل الفوري السلس.
- **عناوين الفواتير**: تم استخدام تدرج أحجام يبدأ من `0.75rem` للنصوص الدقيقة، ويصل إلى `2.25rem` للعناوين.

---

## 4. كيفية التفعيل والاستدعاء

1. **الوضع الداكن تلقائياً**: يتفعل بناءً على إعدادات متصفح أو نظام المستخدم.
2. **الوضع الداكن يدوياً**: بإضافة كلاس `dark` أو السمة `data-theme="dark"` إلى علامة الـ `<html>`:
```javascript
// تشغيل
document.documentElement.classList.add('dark');
// إيقاف
document.documentElement.classList.remove('dark');
```
