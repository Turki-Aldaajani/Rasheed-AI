# المساهمة في رشيد

هذا الدليل يشرح كيف تساهم في المشروع — من اختيار مهمة إلى دمج الـ PR.

## قبل ما تبدأ

```bash
npm install
npm run dev        # http://localhost:3000
npm run typecheck
npm run build
```

## سير العمل

1. **اختر مهمة** من تبويب [Issues](https://github.com/Turki-Aldaajani/Rasheed-AI/issues) — كل مهمة موثّقة بالمشكلة، التفاصيل المطلوبة، الاعتماديات، ومعايير النجاح.
2. **اضغط assign yourself** على المهمة عشان الكل يعرف إنها معك.
3. **اشتغل على فرع مستقل** باسم واضح يعكس المهمة (مثال: `x22-loading-error-empty-states`).
4. **التزم حرفيًا بمعايير النجاح** المكتوبة في المهمة، ولا تعدّل ملفات خارج نطاقها.
5. **اكتب اختبارًا** يثبت نجاح التغيير حيث أمكن.
6. **افتح PR** واكتب في وصفه `Closes #رقم-المهمة` — تُغلق المهمة تلقائيًا بعد الدمج. استخدم [قالب الـ PR](.github/pull_request_template.md) كمرجع لما يُتوقع في الوصف.

## تشتغل بوكيل ذكاء اصطناعي (Claude Code أو غيره)؟

أعطه هذا التوجيه:

> اقرأ Issue رقم N عبر GitHub على `gh issue view N` بالكامل لفهم المهمة ومعيار
> القبول. نفّذها على فرع جديد باسم مناسب والتزم حرفيًا بمعيار القبول. لا تعدّل
> ملفات خارج نطاق المهمة. اكتب اختبارًا يثبت النجاح حيث أمكن، ثم افتح PR بوصفه
> "Closes #N".

## معايير الكود

- الهوية البصرية الحالية ثابتة — راجع [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md) قبل أي تعديل على الألوان أو الخطوط أو المسافات.
- لا قيم مكتوبة داخل الواجهات؛ البيانات في `data/`، وحسابات المحاكاة في `lib/simulation.ts` (راجع [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) لتفاصيل تدفق البيانات).
- كل النصوص الظاهرة للمستخدم بالعربية.

## الليبلات الحالية

`gate` · `infra` · `ai` · `feature` · `ux` · `quality` · `security` · `ops` · `docs` · `bug` — تصف نوع المهمة. أولوية المهمة (MUST/SHOULD/STRETCH) موثّقة في وصف كل Issue ضمن خطة السبرنت الداخلية، لا كليبل منفصل.

## الإبلاغ عن خلل أو اقتراح ميزة

افتح Issue جديدة وستظهر لك قوالب **بلاغ خلل** و**اقتراح ميزة** تلقائيًا. مهام السبرنت المخطط لها مسبقًا تُفتح كـ Issue فارغة (Blank issue) بنفس تنسيق المهام الحالية (المشكلة، التفاصيل المطلوبة، الاعتماديات، معايير النجاح).
